# Batch 04 uncertainty and verification limits

## Geographic evidence

**Jay Mews `way-117010293` and the anonymous Albert Close feature `way-642055723` are artistic completions on mapped footprints.** Inspected photographs provide neighbourhood context, not confirmed target-specific window/roof observations. Their dimensions, elevations, window counts, roof structure and detailing remain estimated. “Albert Close” is a contextual working label.

Princes Gate Court `way-27917475` uses an inspected 2007 courtyard photograph for architectural interpretation. Its oblique viewpoint, vegetation and missing rear/roof coverage limit what can be established. Storey layout, unseen faces, exact heights, openings and roof construction are not measured recovery. Council-indexed proposed upper-floor alterations are not treated as evidence that construction occurred.

All three retain complete mapped footprints in the agreed projected frame, with estimated flat vertical datum. Original door-leaf locations are distinct from wall-plane threshold centres: the approximately 0.16 m source recess is explicitly retained as metadata. Registration consistency and modelling detail do not certify survey accuracy.

## Geometry verification

The current interface report passes 1,004 sampled roof-coverage checks, 120 shared-wall samples and 75 entrance rays. The independent export/reimport check reports zero maximum bounds discrepancy and matching material values. These checks support geometry consistency, coverage and entry clearance in the checked regions; they do not prove every possible intersection, real-world dimensions or accessibility compliance.

The coordinator reports inspecting 15 individual-building views plus the overview. A door central stile was identified/renamed as a door component during the final rebuild. Roof construction was reviewed for holes in the building modules. These statements reflect coordinator/agent review; this documentation agent did not independently render or inspect a new image set.

The Three.js loader/batching report covers **10 distinct replacement building IDs cumulatively**, including **3 new IDs in batch 04**. This is local integration verification, not a live browser screenshot or full-city visual review. Physical simulation fields/masks are not claimed recomputed by this geometry batch.

## Status ownership

Documentation records the available reports and coordinator’s visual review. Final `visual_reviewed`, delivery, progress and remaining-frontier state changes belong to the coordinator. No such machine-readable status file was changed while writing these documents. At the documentation snapshot, `verification.json` still contains `visual_reviewed=false` and `delivered=false`; the later coordinator update is authoritative.
